// 모든 솔리디티 소스 코드는 "version pragma"로 시작해야 하는데, 이는 해당 코드가 이용해야 하는 솔리디티 버전을 선언
// 이후에 새로운 컴파일러 버전이 나와도 기존 코드가 깨지지 않도록 예방

pragma solidity ^0.4.19; //1. 여기에 솔리디티 버전 적기

contract ZombieFactory {
    //2. 여기에 컨트랙트 생성
}